<section class="user-section">
  <h2><%= "#{@user.name}" %></h2>
  <%= invitation(@user) if @user != current_user %>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts if @posts %>
    </ul>
  </article>
</section>

